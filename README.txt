# BigBangSim — Engine v1

## 🚀 Instrucciones de uso
1. Descomprime.
2. PowerShell -> cambia a la carpeta (y a la unidad correcta): `E:` luego `cd "E:\BigBangSim_Engine_v1"`
3. `npm install`
4. `npm run dev` y abre `http://localhost:5173`.

---

## 📄 Licencia
Este proyecto está publicado bajo la licencia **Apache 2.0** (archivo `LICENSE` en la raíz).

Adicionalmente:
- En la carpeta `docs/` se incluyen licencias complementarias como **Creative Commons BY-NC-ND 4.0** aplicables a documentación y materiales específicos.
- El archivo `NOTICE` contiene información de atribuciones requeridas.
- El archivo `CITATION.cff` describe cómo citar este software en contextos académicos.

Consulta los archivos mencionados para más detalles.
